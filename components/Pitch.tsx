import React from 'react';

/**
 * This is an inert component to fix a build-time error.
 * The file was left empty after a refactor, causing the application to crash on load.
 * Populating it with a valid, non-rendering component resolves the issue.
 */
const Pitch: React.FC = () => {
  return null;
};

export default Pitch;
